# Master Database Connection (Fase 1)
DB_MIGRATION_TEMP_URL=postgres://user:password@hostname:port/database

# JWT Secrets (Fase 6)
JWT_SECRET=your_jwt_secret_key_here

# Application Settings
PORT=3000
NODE_ENV=production
